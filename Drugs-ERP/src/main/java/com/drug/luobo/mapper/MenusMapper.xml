<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="com.drug.luobo.mapper.MenusMapper">
      <select id="getAllMenusByRoleId" parameterType="Integer" resultType="Menus" >
             SELECT m.`mid`,m.`pid`,m.`title`,m.`href`,m.`spread`,m.`target`,m.`icon`,m.`available` FROM menus m INNER JOIN menusrole mr ON
				m.`mid`=mr.`mid` INNER JOIN employee e ON
				e.`roleId`=mr.`roleId` WHERE e.`roleId`=#{roleId} AND m.`available`=1
             
      </select>
</mapper>
